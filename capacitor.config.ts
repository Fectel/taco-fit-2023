import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'taco-fit-2023',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
